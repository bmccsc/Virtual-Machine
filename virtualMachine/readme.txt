The packages within this folder represent separate attempts to create a virtual machine.

virtualMachine: first attempt
vm2: second attempt
vm3: third attempt

The classes in each package are incomplete and probably shouldn't be compiled.